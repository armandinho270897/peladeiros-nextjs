// ID fixo do dono do app — só essa conta acessa a fila de aprovação de
// arena. Trocar aqui se a titularidade mudar (não tem UI pra isso de
// propósito, é uma decisão rara e sensível o bastante pra ficar no código).
export const ADMIN_USER_ID = '4d0e403c-e09d-4e53-a180-228ba85a6455';
